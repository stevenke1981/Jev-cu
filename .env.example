# 从 https://openrouter.ai/settings/keys 获取 OpenRouter API key。
# 在项目根目录 .env.local 中填写，或设置同名环境变量；不要提交真实密钥。
# 默认模型：typesafe/jev-1.13
# 默认端点：https://openrouter.ai/api/alpha/decisions
# TYPESAFE_API_KEY 不再作为默认密钥，也不会自动回退到 TypeSafe 直连。
OPENROUTER_API_KEY=
