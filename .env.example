# Jev decision helper only. GPT runs in ChatGPT desktop, not through this key.
# Store the real key in .env.local or the environment; never commit it.
# Default: typesafe/jev-1.13 at OpenRouter Decisions API.
OPENROUTER_API_KEY=
