# 日曆示範

本頁只在使用者明確選擇日曆測試時使用，不自動操作。

在 ChatGPT 桌面使用 GPT 與官方 Computer Use，確認 Calendar 的真實當前月份、切換按鈕及授權。依 runtime.md 選擇符合主機的入口；Windows 每次1步，先 dry-run，再準備提案、於另一個工具呼叫執行並驗收。舊 cua 入口可做2步以內的單一目標。

按鈕索引和月份不得從 fixtures 複製。沒有候選或相符的官方文字樹介面則停止；不使用替代代理、瀏覽器 bridge 或自製 Windows driver。
